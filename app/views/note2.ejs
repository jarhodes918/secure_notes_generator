<!DOCTYPE html>
<html>

<head>
	<!-- Use the 'title' property to render a page title  -->
	<title>Show Note</title>
</head>

<body>
	<ul>
		<li>
		<%subjectArray.forEach(function(sbt) {%>
			<%= note.note %>
			<%}); %>
		</li>
	</ul>
</body>

</html>